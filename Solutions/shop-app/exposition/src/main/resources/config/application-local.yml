#Monitoring endpoints
management:
   endpoints:
      web:
         exposure:
            include: info, health
logging:
  level:
    org.hibernate.SQL: DEBUG
    #org.hibernate.type.descriptor.sql.BasicBinder: TRACE